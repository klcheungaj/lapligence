# True net-alias fixtures

The true-net alias designs in this directory cover IEEE 1800-2009 §10.11:
whole and
constant selected aliases, concatenated aliases, ascending packed ranges,
continuous and primitive drivers, port links, force/release, and conflicting
drivers. The owning suite runs every fixture through `llg` and `llg --no-opt`.
