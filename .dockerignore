# Exclude the shared build-cache folder from the main build context.
# It is passed separately via --build-context build-cache=./build-cache
build-cache/

# Rust build output
target/

# Local agent records and archived vendor work are not build inputs.
persistence/
